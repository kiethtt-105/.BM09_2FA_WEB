# ===========================================================

# ===========================================================

# --- Django core ---
SECRET_KEY={key của bạn}	#
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,{link-ngrok}.ngrok-free.dev	#

# --- CSRF  ---
CSRF_TRUSTED_ORIGINS=http://localhost:8000,{link-ngrok}.ngrok-free.dev	#
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False

# --- Database PostgreSQL ---
DB_NAME=huit_db {tên dtb}
DB_USER=postgres
DB_PASSWORD={pass  postgres}	#
DB_HOST=localhost
DB_PORT=5432

# local dùng prefer, production đổi thành require
DB_SSL_MODE=prefer

# --- Email Gmail ---
EMAIL_HOST_USER={địa chỉ email -dùng để gửi otp 	#
EMAIL_HOST_PASSWORD={pass app } # Ko phải pass tài khoản

# --- SSO ---
SSO_SECRET_KEY= {tạo chuỗi secret_key32}	#
WEB_SSO_CALLBACK_URL=http://localhost:8001/sso/callback/ #dùng để login sso app B

# --- Mã hóa ---
ENCRYPTION_KEY={tạo chuoioxhash32 tuong tự như trên}
